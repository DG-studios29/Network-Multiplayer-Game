using UnityEngine;
using Mirror;

public class NetTempFinder : NetworkBehaviour
{

    [SerializeField] private CannonHolder cannonHolder;
    [SerializeField] private CannonLinq cannonLinq;
    [SerializeField] private CannonHUD cannonHUD;
    [SerializeField] private CanvasLoadHUD canvasLoadHUD;
    [SerializeField] private PresetsHolder presetsHolder;


    private void Awake()
    {
   


    }


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
